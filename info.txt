pandas (
    python -m pip install -U pip
    pip install -U setuptools
    pip install -U virtualenv
    pip install pandas)
numpy (in pandas)
scipy (sudo apt-get install gfortran libopenblas-dev liblapack-dev)
(http://www.lfd.uci.edu/~gohlke/pythonlibs/#scipyexit)
matplotlib
er
